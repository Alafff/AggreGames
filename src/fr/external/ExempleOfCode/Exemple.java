/**
 * 
 */
package fr.external.ExempleOfCode;

/**
 * @author Alafff
 *
 */
public class Exemple {
	/*
	 for (int i = 0; i < games.size(); i++) {
         Game g = games.get(i);
         String string = g.toString();
         doShit(string);
     }
     
     ag.stream().map(g::toString).forEach( string-> doShitWithString(string));

     ag.stream().forEach(item -> doSomethingWith(item.toString()));
     
     
     */
}
